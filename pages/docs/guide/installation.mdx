import { Tabs } from 'nextra/components'

# Installation

<Tabs items={['npx', 'npm', 'yarn', "pnpm"]}>
  <Tabs.Tab>
    ```shell
    npx clingon@latest <command>
    ```
  </Tabs.Tab>
  
  <Tabs.Tab>
    - Install

        ```shell
        npm install -g clingon@latest
        ```
    - Run

        ```shell
        clingon <command>
        ```

  </Tabs.Tab>
  
  <Tabs.Tab>
    - Install
    
        ```shell
        yarn add -g clingon@latest
        ```
    - Run
    
        ```shell
        clingon <command>
        ```
    </Tabs.Tab>

  <Tabs.Tab>
    - Install
    
        ```shell
        pnpm add -g clingon@latest
        ```
    - Run
    
        ```shell
        clingon <command>
        ```
  </Tabs.Tab>
</Tabs>

See the next page to know about available commands
